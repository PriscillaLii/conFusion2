// this is the baseURL at which my JSON server is accessible
// angular provides a way of supplying values like this to the rest of your application 
// by configuring a provider within your app module.
export const baseURL = 'http://localhost:3000/'